# BookManager

ASP.NET Core Web API приложение для управления мероприятиями.

## Требования

- .NET 8 SDK или выше

## Запуск

1. Клонировать репозиторий:
```bash
git clone <URL_РЕПОЗИТОРИЯ>
```

2. Перейти в папку проекта:
```bash
cd BookManager
```

3. Восстановить зависимости:
```bash
dotnet restore
```

4. Собрать проект:
```bash
dotnet build
```

5. Запустить проект:
```bash
dotnet run
```

## Swagger

После запуска Swagger доступен по адресу:

```text
http://localhost:<port>/swagger
```

или

```text
https://localhost:<port>/swagger
```

Точный адрес будет показан в консоли после запуска приложения.

## API

### GET /events
Возвращает список всех мероприятий.

### GET /events/{id}
Возвращает мероприятие по идентификатору.  
Если мероприятие не найдено, возвращается `404 Not Found`.

### POST /events
Создаёт новое мероприятие.  
При успешном создании возвращается `201 Created`.

Пример тела запроса:

```json
{
  "title": "Team meeting",
  "description": "Sprint planning",
  "startAt": "2026-04-22T10:00:00",
  "endAt": "2026-04-22T11:00:00"
}
```

### PUT /events/{id}
Полностью обновляет мероприятие по идентификатору.  
Если мероприятие не найдено, возвращается `404 Not Found`.

### DELETE /events/{id}
Удаляет мероприятие по идентификатору.  
Если мероприятие не найдено, возвращается `404 Not Found`.

## Особенности реализации

- Отключено предупреждение warning CS1591 в BookManager.csproj чтобы не писать комментарии для всех публичных видимых типов