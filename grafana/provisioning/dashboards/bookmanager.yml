apiVersion: 1

providers:
  - name: BookManager
    orgId: 1
    folder: BookManager
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
